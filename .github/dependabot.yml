version: 2

updates:
  - package-ecosystem: github-actions
    directories:
      - '/'
    schedule:
      interval: weekly
    cooldown:
      default-days: 3
    commit-message:
      prefix: 'chore'
      include: 'scope'
    labels:
      - 'dependencies'
      - 'automated'
    groups:
      github-actions-security:
        applies-to: security-updates
        patterns:
          - '*'
      github-actions:
        applies-to: version-updates
        patterns:
          - '*'

  - package-ecosystem: docker
    directories:
      - '/'
      - '/test/e2e/localgit'
    schedule:
      interval: weekly
    cooldown:
      default-days: 3
    commit-message:
      prefix: 'chore'
      include: 'scope'
    labels:
      - 'dependencies'
      - 'automated'
    groups:
      docker:
        applies-to: version-updates
        patterns:
          - '*'

  - package-ecosystem: npm
    directories:
      - '/'
      - '/packages/git-proxy-cli'
      - '/plugins/git-proxy-plugin-samples'
      - '/test/fixtures/test-package'
      - '/website'
    schedule:
      interval: weekly
    versioning-strategy: increase-if-necessary
    cooldown:
      # Only applies to version updates (security updates are never delayed)
      semver-patch-days: 3
      semver-minor-days: 3
      semver-major-days: 7
    open-pull-requests-limit: 10
    commit-message:
      prefix: 'chore'
      include: 'scope'
    labels:
      - 'dependencies'
      - 'automated'
    groups:
      npm-security:
        applies-to: security-updates
        patterns:
          - '*'
      npm-non-major:
        applies-to: version-updates
        patterns:
          - '*'
        update-types:
          - minor
          - patch
