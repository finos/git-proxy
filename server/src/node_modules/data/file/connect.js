const fs = require('fs');
const db = require('diskdb');

if (!fs.existsSync('./data')) fs.mkdirSync('./data');

const collection = async (collection) => {
  
  return await db.connect('./data/db', [collection]);
}

module.exports.collection = collection;