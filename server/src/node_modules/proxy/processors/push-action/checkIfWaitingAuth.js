const Step = require('proxy/actions').Step;
const data = require('proxy/data')

const exec = (req, action) => {
  const step = new Step('waitingAuthCheck');

  const id = 
  
  data.getRequest()
  
  
  step.setAsyncBlock(`Your push request is waiting authorisation, tracking id http://localhost:8080/requests/${action.id}`);
  action.addStep(step);
  return action;
};

exec.displayName = 'blockForAuth.exec';
exports.exec = exec;
